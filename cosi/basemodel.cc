#include <cosi/basemodel.h>

namespace cosi {

std::map< genid, ModelState > getBaseModel( DemographyP demography, HistEventsP histEvents ) {
	std::map< genid, ModelState > gen2mdl;

	return gen2mdl;
}

}  // namespace cosi
